# the version of black is specified also in the stylish.yml github workflow;
# please update the version there in case it is bumped here
black==24.3.0
flake8
# the file can probably be removed and moved to optional dependencies in pyproject.toml 
# also with pytest as an optional dev requirement
